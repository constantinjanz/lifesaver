<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme: platform Material dark, no action bar (all UI is Compose). -->
    <style name="Theme.Lifesaver" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Intervention / block screens: full-screen, translucent window so the circular
         reveal reads as an overlay winning the race against the target app's first frame. -->
    <style name="Theme.Lifesaver.Intervention" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
